import buildFullPath from 'axios/lib/core/buildFullPath';
import buildURL from 'axios/lib/helpers/buildURL';

/**
 * @type {buildFullPath:Function,buildURL:Function}
 */
export { buildFullPath, buildURL };
