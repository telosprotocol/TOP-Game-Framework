import { storeTypeMap } from '../../util';
import { MODULE_NAME } from './MODULE_NAME';
import { UNIVERSE_MUTATIONS_TYPES } from './UNIVERSE_MUTATIONS_TYPES';

export const UNIVERSE_MUTATIONS = storeTypeMap(
  MODULE_NAME,
  UNIVERSE_MUTATIONS_TYPES
);
